apiVersion: v1
kind: Pod
metadata:
  annotations:
    example: kubedock 
spec:
  containers:
  - resources:
      requests:
        memory: "64Mi"
        cpu: "10m"
    securityContext:
      allowPrivilegeEscalation: false